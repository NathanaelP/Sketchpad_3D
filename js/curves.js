// Phase 2: Ramer-Douglas-Peucker simplification + Catmull-Rom spline rendering

export function simplifyPoints(points, epsilon) {
  // Stub: returns input unchanged until Phase 2
  return points;
}

export function catmullRomCurve(points, segments) {
  // Stub: returns input control points until Phase 2
  return points;
}
